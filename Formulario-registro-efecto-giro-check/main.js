const nombre = document.getElementById("name");
const apellido = document.getElementById("lastname");
const correo = document.getElementById("email");
const celular = document.getElementById("mobil");
const contraseña = document.getElementById("pass");
const confircontraseña = document.getElementById("confpass");
const terminos = document.getElementById("condicion");
const form = document.getElementById("formulario");
const lisinput = document.querySelectorAll(".form-input")

// form.addEventListener("submit", (e) =>{
//      e.preventDefault();

//      if(nombre.value.length < 1 || nombre.value.trim() == "") {
//        let elemento =document.querySelector(".name");
//        elemento.lastChild.innerHTML = "Nombre no Valido"
//      }
// })
